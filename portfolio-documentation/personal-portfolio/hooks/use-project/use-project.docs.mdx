---
description: A hook that is used to fetch project information via an id
labels: ['hook', 'fetch project hook']
---

import { useProject } from './use-project';

A hook that fetched a project by `id`.

### Hook Usage.

```js
const { project, loading } = useProject({ id: string });
```
