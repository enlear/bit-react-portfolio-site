---
description: A context component to help store application settings.
labels: ['React Context', 'State', 'Application Settings']
---

import { SettingsProvider, SettingsConsumer } from './settings-context';
import { darkTheme } from '@showoff/design.theme.dark-theme';
import { lightTheme } from '@showoff/design.theme.light-theme';
import { ThemeProvider } from '@showoff/design.theme.theme-provider';
import { Button } from '@showoff/design.inputs.button';
import { Heading } from '@showoff/design.typography.heading';

A context component to provides the app preferences.

### Component usage

```js
<SettingsProvider>
  <SettingsConsumer>{({ theme, toggleTheme }) => <></>}</SettingsConsumer>
</SettingsProvider>
```

### Render settings context

```js live
<SettingsProvider>
  <SettingsConsumer>
    {({ theme, toggleTheme }) => (
      <ThemeProvider theme={theme === 'dark' ? darkTheme() : lightTheme()}>
        <Heading value={'Settings Context'} />
        <Button value={'Toggle Theme'} onClick={toggleTheme} />
      </ThemeProvider>
    )}
  </SettingsConsumer>
</SettingsProvider>
```
