---
description: A form that is compiled to submit contact messages
labels: ['form', 'contact form', 'support form']
---

import { ContactForm } from './contact-form';
import { ThemeProvider } from '@showoff/design.theme.theme-provider';
import { lightTheme } from '@showoff/design.theme.light-theme';

A component renders a contact form
Use with `ThemeProvider` to apply relevant stylings

### Component usage

```js
<ThemeProvider theme={lightTheme()}>
  <ContactForm onSubmit={handleSubmit} />
</ThemeProvider>
```

### Render hello world!

```js live
<ThemeProvider theme={lightTheme()}>
  <ContactForm onSubmit={(values) => console.log(values)} />
</ThemeProvider>
```
