---
description: A widget that implements an MUI Loading Button.
labels: ['UI', 'Button', 'Widget']
---

import { Button } from './button';
import { darkTheme } from '@showoff/design.theme.dark-theme';
import { ThemeProvider } from '@showoff/design.theme.theme-provider';

A component that renders a button component.
Use with `ThemeProvider`

### Component usage

```js
<ThemeProvider theme={darkTheme()}>
  <Button value="Dark Button" />
</ThemeProvider>
```

### Render hello world!

```js live
<ThemeProvider theme={darkTheme()}>
  <Button value="Dark Button" />
</ThemeProvider>
```
