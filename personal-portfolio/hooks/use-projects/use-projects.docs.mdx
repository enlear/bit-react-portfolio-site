---
description: A hook that is used to fetch all projects.
labels: ['data fetching', 'data initializing']
---

import { useProjects } from './use-projects';

A hook that fetches the projects.

### Component usage

```js
const { projects, loading } = useProjects();
```
